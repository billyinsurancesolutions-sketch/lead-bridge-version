# Lead Bridge — version marker

One file, `version.json`, holding the current Lead Bridge version. The extension reads it to
tell its owner when a newer build exists. Public on purpose: it carries a version number and
nothing else — no code, no keys, no lead data. The extension itself lives in a private repo.
